/*
    This script is meant for when you're starting a new Bitnode.
*/

// set sleeves task to 'Shock Recovery'
// run early-hack-template.js -t 10
// start committing Shoplift
    // optionally, scale this based on % chance of success of future crimes and change crimes as needed
// while(ns.getPlayer().money < 200k){
//      if(ns.hacknet.getPurchaseNodeCost() < 200000){ns.hacknet.purchaseNode()}
//      upgrade hacknet nodes
// }
// once you have 200k, buy TOR router
// buy BruteSSH.exe
// run auto-install-early-hack-template.js
// run purchase-server-8gb.js
// while(faction != Slum Snakes)
    //check if each of these is available, and if so, buy them
        // buy FTPCrack.exe
            // if bought, run auto-install-early-hack-template_v3.js
        // buy relaySMTP.exe
            // if bought, run auto-install-early-hack-template_v3.js
        // buy HTTPWorm.exe
            // if bought, run auto-install-early-hack-template_v3.js
        // buy SQLInject.exe
            // if bought, run auto-install-early-hack-template_v3.js
    // check if we have 5x the cost of a 1tb server.  If so, run purchase-server-1tb.js
    // check if ns.singularity.getCrimeChance("Homicide") > 20 && faction != Sector-12, if so start committing Homicide
    // check if ns.singularity.getCrimeChance("Homicide") < 80 && we can join Sector-12 && faction != Sector-12, if so then start Field Work for Sector-12
    // check if ns.singularity.getCrimeChance("Homicide") > 80, if so then start committing Homicide
    // for sleeve of sleeves:
        // is shock level < 10?  If so, train combat stats
            // for each combat stat, is it 70? if so, move on to next one
        // is shock level < 10 and combat stats are all 70? then start homicide
    // check if our karma is at -54000, and if so, join Slum Snakes and create a gang - this marks end of early game